paths:
    migrations: %%PHINX_CONFIG_DIR%%/../setup/db/migrations
    seeds: %%PHINX_CONFIG_DIR%%/../setup/db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development
    development:
        adapter: mysql
        host: localhost
        name: ziemes
        user: root
        pass: 'nsseaplp'
        port: 3306
        charset: utf8
        collation: utf8mb4_unicode_ci

